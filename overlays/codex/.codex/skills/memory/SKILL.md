---
name: memory
description: Архитектура памяти проекта между сессиями — нативная иерархия инструк-файлов рантайма и память между сессиями, дисциплина (<200 строк/файл, тонкий индекс + ленивые topic-файлы, tiered loading ради качества), чекпойнт перед компакцией, указатели на routines/loop. Используй, когда организуешь знания/заметки проекта, настраиваешь память или контекст разрастается.
---

Как проект помнит контекст между сессиями. **Полное — в файле `core/memory.md`** (от корня проекта): прочитай его целиком. (Discovery у Codex — этот SKILL.md в `.codex/skills/memory/`.)

Кратко:

- Две системы памяти: **инструк-файл рантайма** (твои правила/факты, в репо — на Codex это `AGENTS.md`) + **память рантайма между сессиями** (вне гита).
- Дисциплина **ради качества рассуждения, не экономии**: <200 строк/файл, тонкий индекс + ленивые topic-файлы, грузить только релевантное, не дублировать.
- Дорогие-но-качественные шаги (вторая модель в панели, verification passes, multi-agent) — дефолт, не «по бюджету». Дисциплина контекста экономит место под рассуждение, а не режет глубину.
- Опционально: recovery-чекпойнт перед компакцией контекста; фоновая/итеративная работа; человеческий гейт на high-stakes. Конкретные хуки/команды для них — рантайм-специфичны.
- **Когда НЕ:** не для разовой заметки в чате — только когда организуешь знание МЕЖДУ сессиями, настраиваешь память или контекст разрастается.
