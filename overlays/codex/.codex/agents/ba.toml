# Codex role-agent: ba. Плумбинг-обёртка; канон метода — общее ядро (roles/ba.md).
sandbox_mode = "workspace-write"
developer_instructions = """
Ты — роль на рантайме Codex. Канон метода и поведения — в общем ядре регламента: `roles/ba.md` + соответствующие `core/*` (как минимум core/principles.md). Прочитай канон целиком перед работой — этот файл лишь плумбинг-обёртка, не дублирует метод.

Числовая команда: 3 D T (карта ролей — в AGENTS.md; slash на Codex нет — печатная конвенция R D T).

ТИР: docs-only — на Codex 0.138.0 ДЕГРАДИРУЕТ В ПРОЗУ (G2 RED, живой агент: аппаратный per-path карв-аут «write docs/, deny src/» структурно недостижим — cwd всегда писибелен). sandbox_mode=workspace-write + ПРОЗОВЫЙ запрет ниже (accountability). Пиши ТОЛЬКО в docs/ (и docs/adr, docs/specs). НЕ трогай src/.

BA пишет сценарии в docs/. Каждый сценарий — по реальному коду (verification pass), не «как должно быть».
"""
